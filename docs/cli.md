# Команды CLI

## Указание проекта и организации

Везде, где команда принимает `--project` / `-p`, проект задаётся по ID или имени. Дополнительно имя можно префиксовать организацией:

```bash
# Проект из организации по умолчанию (slug из конфига)
cveta2 fetch -p "Мой проект" -o output/

# Проект из другой организации
cveta2 fetch -p "other-org/Мой проект" -o output/

# Проект из личного пространства (вне организаций)
cveta2 fetch -p "/Мой проект" -o output/
```

При интерактивном выборе проекта список показывается **постранично по организациям**: первая страница — организация из конфига, пункт «→ Следующая организация» переключает страницы (включая личное пространство), «↻ Обновить список» перечитывает проекты всех организаций с CVAT.

## Настройка

### Настроить подключение к CVAT (`cveta2 setup`)

Интерактивный мастер настройки подключения к CVAT: хост, slug организации, логин/пароль. Результат сохраняется в `~/.config/cveta2/config.yaml`.

Slug организации нужно либо указать, либо явно подтвердить его отсутствие (вопрос повторяется, пока не получен ответ) — это защищает от случайной работы вне организации.

```bash
cveta2 setup

# Указать другой путь к конфигу
cveta2 setup --config /path/to/config.yaml
```

### Настроить кэш изображений (`cveta2 setup-cache`)

Интерактивная настройка путей кэширования изображений для всех известных проектов. Сначала запрашивается **корневая директория кэша**. Если она указана, команда показывает вычисленные пути «корень/имя_проекта» для всех проектов и предлагает применить их **одним подтверждением** — без опроса по каждому проекту. При отказе (или если корень не указан) путь запрашивается для каждого проекта отдельно; нажмите Enter, чтобы принять значение по умолчанию или пропустить проект.

После путей изображений команда настраивает секцию `cache`:

- указанный корень сохраняется как `cache.images_root` — глобальный корень для проектов, которых нет в `image_cache`;
- запрашивается **локальный корень кэша аннотаций задач** (`cache.tasks_root`; по умолчанию `~/.cache/cveta2/task_annotations`);
- по желанию — по проектам: **игнорируемый префикс S3** (`ignored_prefix`) и **S3-путь кэша задач** (`task_cache_s3`), см. разделы про изображения и кэш аннотаций ниже.

Если локальный кэш проектов пуст — автоматически загружает список с CVAT.

- **`--reset`** — переспросить путь для каждого проекта, используя только «корень/имя_проекта» как значение по умолчанию (игнорировать уже заданные пути).
- **`--list`** — вывести текущие пути кэша и настройки секции `cache` из конфига и выйти (без запросов и без CVAT).

```bash
cveta2 setup-cache

# Указать другой путь к конфигу
cveta2 setup-cache --config /path/to/config.yaml

# Показать текущие пути кэша
cveta2 setup-cache --list
```

### Настроить публикацию в ClearML (`cveta2 setup-clearml`)

Интерактивная настройка маппинга проектов CVAT на ClearML Dataset (см. раздел [«Публикация в ClearML»](images-and-cache.md#публикация-в-clearml)). Команда включает/выключает интеграцию и для каждого известного проекта запрашивает ClearML project и dataset name; Enter — пропустить проект или принять текущее значение. Список проектов берётся из локального кэша — если он пуст, сначала выполните `cveta2 setup-cache`.

```bash
cveta2 setup-clearml

# Показать текущий маппинг и выйти
cveta2 setup-clearml --list

# Указать другой путь к конфигу
cveta2 setup-clearml --config /path/to/config.yaml
```

## Выгрузка из CVAT

### Выгрузить все аннотации проекта (`cveta2 fetch`)

Выгрузка **всех** bbox-аннотаций и удалённых изображений из проекта CVAT. Задачи выгружаются параллельно — до `network.cvat_workers` одновременно (см. [Параллельность передач](configuration.md#параллельность-передач)) — и каждая сохраняется как промежуточный CSV в `output/.tasks/task_{id}.csv`, после чего результаты объединяются и разбиваются на три CSV-файла + список удалённых. Если папка `--output-dir` уже существует, в интерактивном режиме будет предложено перезаписать, указать другой путь или отменить.

```bash
# По ID проекта
cveta2 fetch --project 123 -o output/

# По имени проекта
cveta2 fetch -p "Имя проекта" -o output/

# Интерактивный выбор проекта (из кэша; в списке есть опция обновить с CVAT)
cveta2 fetch -o output/

# Дополнительно сохранить полный (необработанный) CSV
cveta2 fetch -p 123 -o output/ --raw

# Не публиковать результат в ClearML (см. раздел «Публикация в ClearML»)
cveta2 fetch -p 123 -o output/ --no-clearml
```

Флаги, общие с `fetch-task` (`--completed-only`, `--no-images`, `--images-dir`,
`--save-tasks`, `--no-cache`, `--force`), собраны в разделе
[Общие флаги выгрузки](#общие-флаги-выгрузки) ниже.

Завершённые задачи по умолчанию берутся из кэша аннотаций — см.
[«Кэш аннотаций задач»](images-and-cache.md#кэш-аннотаций-задач).

### Выгрузить конкретные задачи (`cveta2 fetch-task`)

Выгрузка bbox-аннотаций для **конкретных задач** проекта. Использует ту же логику поштучной выгрузки задач, что и `fetch` (промежуточные CSV в `output/.tasks/`). В отличие от `fetch`, не разбивает результат на dataset/obsolete/in_progress — записывает единый `dataset.csv` и `deleted.csv` в указанную директорию. Задачи указываются по ID или имени через `-t`, либо выбираются интерактивно (checkbox с поиском).

```bash
# Одна задача по ID
cveta2 fetch-task -p 123 -t 456 -o output/

# Несколько задач
cveta2 fetch-task -p 123 -t 456 789 -o output/

# По имени задачи (регистр не важен)
cveta2 fetch-task -p 123 -t "Партия 3" "Партия 4" -o output/

# Без -p с числовыми ID задач — проект определяется автоматически по задаче
cveta2 fetch-task -t 456 -o output/

# Интерактивный выбор задач из списка (checkbox с мульти-выбором и поиском)
cveta2 fetch-task -p 123 -t -o output/

# Без -t — тоже интерактивный выбор
cveta2 fetch-task -p 123 -o output/
```

Если `--project` не указан, но заданы числовые ID задач, проект выводится из первой такой задачи (интерактивный выбор проекта не понадобится). Задачи, заданные только именами, без `-p` не резолвятся — будет показан интерактивный выбор проекта.

В директории `output/` появятся `dataset.csv` и `deleted.csv`. Подробнее — в
[DATASET_FORMAT.md](../DATASET_FORMAT.md).

### Общие флаги выгрузки

`fetch` и `fetch-task` принимают один и тот же набор флагов, отвечающих за
фильтр задач, изображения и кэш аннотаций:

| Флаг | Что делает |
|---|---|
| `--completed-only` | Брать только задачи, все jobs которых на `acceptance`/`completed` |
| `--no-images` | Не скачивать изображения |
| `--images-dir DIR` | Куда скачивать изображения (переопределяет `image_cache` на этот запуск) |
| `--save-tasks` | Оставить промежуточные CSV задач в `output/.tasks/` (по умолчанию удаляются) |
| `--no-cache` | Полностью отключить кэш аннотаций: ни чтения, ни записи, ни синхронизации с S3 |
| `--force` | Перечитать все задачи с CVAT и обновить кэш |

```bash
cveta2 fetch -p 123 -o output/ --completed-only --no-images
cveta2 fetch -p "coco8-dev" -o output/ --images-dir /mnt/data/coco8
cveta2 fetch-task -p 123 -t 456 -o output/ --save-tasks --force
```

Подробнее об изображениях и кэше — в
[Изображения, кэш и публикация](images-and-cache.md).

### Что нового в проекте (`cveta2 whats-new`)

Показывает задачи, завершённые **после** тех, что уже есть в выгруженном `dataset.csv`. Дата отсечки — максимальная `task_updated_date` среди строк завершённых задач, то есть тех, у которых все строки имеют `job_stage = acceptance` и `job_state = completed` (если таких нет — среди всех строк). Задачи, чей `task_id` уже присутствует в CSV, помечаются как `(обновлена)`.

> **Внимание:** команде нужны колонки `job_stage` и `job_state`. В CSV, выгруженных до их появления (там была колонка `task_status`), их нет — такой файл нужно выгрузить заново через `fetch`.

```bash
cveta2 whats-new -p "Мой проект" -d output/dataset.csv

# Интерактивный выбор проекта (без -p)
cveta2 whats-new -d output/dataset.csv
```

## Изображения

### Синхронизировать изображения из S3 (`cveta2 s3-sync`)

Синхронизация изображений из S3 cloud storage в локальный кэш. Охватываются все проекты, у которых есть запись в `image_cache` **или** в `cache.projects` — объединение двух секций, а не только первая. Скачивает только отсутствующие файлы — никогда не загружает и не удаляет ничего на S3.

```bash
# Синхронизировать все настроенные проекты
cveta2 s3-sync

# Только один конкретный проект
cveta2 s3-sync -p coco8-dev

# Разово переопределить источник синхронизации (требует --project)
cveta2 s3-sync -p coco8-dev --root s3://other-bucket/some/prefix
cveta2 s3-sync -p coco8-dev --root some/prefix
```

Источник синхронизации по умолчанию — cloud storage проекта в CVAT. Его можно переопределить:

- **`--root`** — на один запуск; принимает `s3://bucket/prefix` или просто `prefix` (бакет остаётся бакетом проекта). Имеет приоритет над конфигом. Требует `--project`.
- **Секция `sync_roots` в конфиге** — постоянное переопределение для проекта (см. [«Конфигурация»](configuration.md#файл-конфигурации)). Действует и на `s3-sync`, и на скачивание изображений при `fetch`. На `upload` не влияет — загрузка всегда идёт в хранилище проекта.

## Загрузка в CVAT

### Создать задачу и загрузить аннотации (`cveta2 upload`)

Создание задачи в CVAT из `dataset.csv`: выбор классов (флагом `--labels` или интерактивно), загрузка изображений на S3 и создание задачи с cloud storage. Кадры в созданной задаче идут **в порядке строк CSV**. Bbox-аннотации из CSV автоматически переносятся в новую задачу (фрейм-маппинг читается из CVAT `data_meta`, а не из порядка файлов).

```bash
# Минимальный вызов — проект и CSV обязательны
cveta2 upload -p "Мой проект" -d output/dataset.csv

# По ID проекта
cveta2 upload -p 123 -d output/dataset.csv

# Классы без интерактивного выбора (спецзначение __no_annotation__ — кадры без аннотаций)
cveta2 upload -p "Мой проект" -d output/dataset.csv --labels car person

# Все классы сразу (включая кадры без аннотаций), без перечисления
cveta2 upload -p "Мой проект" -d output/dataset.csv --labels all

# Исключить изображения, которые уже в работе
cveta2 upload -p "Мой проект" -d output/dataset.csv --in-progress output/in_progress.csv

# Указать директорию с изображениями и имя задачи
cveta2 upload -p "Мой проект" -d output/dataset.csv --image-dir /mnt/data --name "Партия 3"

# Создать задачу и сразу отметить как completed
cveta2 upload -p "Мой проект" -d output/dataset.csv --complete

# Пометить все загруженные изображения удалёнными в созданной задаче
cveta2 upload -p "Мой проект" -d output/dataset.csv --mark-all-deleted

# Продолжить прерванную загрузку (тот же CSV и те же --labels)
cveta2 upload -p "Мой проект" -d output/dataset.csv --labels car --resume

# Интерактивный выбор проекта (без -p)
cveta2 upload -d output/dataset.csv
```

Процесс:

1. Чтение `dataset.csv` и (опционально) `in_progress.csv` для исключения занятых изображений
1. Выбор классов (`instance_label`): через `--labels` или интерактивный checkbox — изображения без аннотаций тоже можно включить. Порядок интерактивных запросов: проект → имя задачи → классы; после их заполнения в stdout печатается полная команда с флагами для повторного запуска без интерактива
1. Поиск изображений в `--image-dir` и кэше проекта — **рекурсивно** по подпапкам (совпадение по имени файла; при дубликатах побеждает лексикографически последний путь)
1. Загрузка недостающих изображений на S3 (уже загруженные пропускаются; новые изображения помещаются в подпапку по месяцу: `YYYY-MM/img.jpg`)
1. Создание задачи в CVAT с cloud storage и автоматическим разбиением на jobs
1. Загрузка bbox-аннотаций в новую задачу (привязка по `image_name` → `frame_id` из CVAT)
1. Создание issues из строк с `issue_state="new"` и непустым `issue_text` (см. [DATASET_FORMAT.md](../DATASET_FORMAT.md)). Issue прикрепляется к bbox своей строки — каждый bbox получает собственный issue (полностью одинаковые строки создаются один раз); строки с `issue_text`, но без полного bbox, пропускаются с предупреждением (полнокадровых issues больше нет)

**Выбор классов работает по кадрам**: выбранная метка отбирает изображения, на которых она встречается, и загружаются **все** аннотации этих изображений — включая сопутствующие метки, которые вы не выбирали. Все метки загружаемых кадров (в том числе сопутствующие) проверяются на существование в проекте CVAT.

Флаг `--mark-all-deleted` после создания задачи помечает **все** загруженные кадры удалёнными. Без него удалёнными помечаются только изображения со строками `instance_shape="deleted"` из CSV.

CSV, состоящий только из удалённых кадров (например, `deleted.csv`), загружается как есть: выбирать нечего, поэтому шаг выбора классов пропускается, а все кадры задачи помечаются удалёнными.

#### Продолжение прерванной загрузки (`--resume`)

Загрузка больших датасетов может прерваться на стороне CVAT: обрыв связи во время обработки изображений, ошибка сервера при загрузке аннотаций, снятый процесс. Повторный запуск обычной командой создаст **вторую** задачу с тем же именем; `--resume` вместо этого продолжает прерванную.

```bash
cveta2 upload -p "Мой проект" -d output/dataset.csv --labels car --resume
```

Перед обращением к CVAT команда записывает состояние запуска в `~/.cache/cveta2/uploads/project_<id>/`, а id созданной задачи сохраняет сразу после её создания — до того, как начнётся долгая привязка изображений. Файл удаляется после успешного завершения.

Состояние хранит **намерение**, а не список выполненного: что уже сделано, `--resume` выясняет, читая CVAT: 

| Этап | Что происходит при `--resume` |
|---|---|
| S3 | Уже загруженные изображения пропускаются; пути, выбранные первым запуском, сохраняются (иначе смена месяца увела бы оставшиеся файлы в другую папку) |
| Задача | Кадров столько же, сколько нужно — задача переиспользуется; кадров нет — задача удаляется и создаётся заново; другое число — команда останавливается и предлагает удалить задачу вручную |
| Аннотации | Если в задаче уже есть аннотации, они не загружаются повторно (CVAT **добавляет** аннотации, а не заменяет — второй проход удвоил бы все bbox) |
| Issues | Сравниваются с уже существующими по паре «кадр + текст»; создаются только недостающие |
| Удалённые кадры, `--complete` | Выполняются заново — обе операции идемпотентны |

Запуск с `--resume` ищет прерванную загрузку **того же набора кадров и тех же классов**. Если такой нет, команда покажет список незавершённых загрузок проекта — скорее всего, был передан другой CSV или другой `--labels`.

Обычный запуск (без `--resume`) при наличии незавершённой загрузки того же набора предупредит, что её состояние будет забыто.

Если часть изображений не удалось загрузить на S3, задача **не создаётся**: иначе в неё попали бы ссылки на несуществующие файлы, и ошибка всплыла бы только при обработке данных — уже после создания задачи.

Количество изображений на job настраивается через `upload.images_per_job` в конфиге (по умолчанию 100), качество изображений в создаваемой задаче — через `upload.image_quality` (по умолчанию 100).

## Работа с датасетами

### Объединить два датасета (`cveta2 merge`)

Слияние двух CSV-файлов с аннотациями — например, старого `dataset.csv` с новой выгрузкой после переразметки.

```bash
# Базовое слияние — для общих изображений побеждает new
cveta2 merge --old old/dataset.csv --new new/dataset.csv -o merged.csv

# С учётом удалённых изображений
cveta2 merge --old old/dataset.csv --new new/dataset.csv --deleted new/deleted.csv -o merged.csv

# Разрешение конфликтов по дате обновления задачи (побеждает более свежая)
cveta2 merge --old old/dataset.csv --new new/dataset.csv --by-time -o merged.csv
```

При конфликтах (изображение есть в обоих файлах) по умолчанию побеждает `--new`. С `--by-time` — побеждает файл с более свежей `task_updated_date`. Изображения из `--deleted` исключаются. Если у победившей стороны `split` пуст, а у `--old` задан — значение переносится автоматически.

### Конвертировать между форматами (`cveta2 convert`)

Конвертация между cveta2 CSV и форматами детекции. Поддерживает три режима: CSV → YOLO (`--to-yolo`), YOLO → CSV (`--from-yolo`) и CSV → COCO (`--to-coco`).

**CSV → YOLO** (`--to-yolo`):

```bash
# Конвертировать dataset.csv в YOLO формат (изображения копируются через reflink/copy)
cveta2 convert --to-yolo -d dataset.csv -o yolo_dataset/

# Использовать символические ссылки вместо копирования
cveta2 convert --to-yolo -d dataset.csv -o yolo_dataset/ --link-mode symlink

# Указать дополнительную директорию с изображениями
cveta2 convert --to-yolo -d dataset.csv -o yolo_dataset/ --image-dir /mnt/data/images

# Доступные режимы --link-mode: auto (по умолчанию), reflink, hardlink, symlink, copy
```

В режимах `auto` и `reflink` при недоступности reflink (например, при копировании между разными файловыми системами) выполняется обычное копирование; предупреждение выводится один раз.

Создаёт структуру директорий для ultralytics:

```
yolo_dataset/
  images/{train,val,test}/   -- изображения
  labels/{train,val,test}/   -- .txt файлы с аннотациями (class_id xc yc w h)
  dataset.yaml               -- конфиг датасета
```

Каждое изображение должно иметь заполненное поле `split` (`train`/`val`/`test`). Изображения без аннотаций (`instance_shape="none"`) создают пустые `.txt` файлы. Русские названия меток сохраняются в `dataset.yaml` в нечитаемом виде.

**YOLO → CSV** (`--from-yolo`):

```bash
# Конвертировать YOLO датасет (с dataset.yaml) в CSV
cveta2 convert --from-yolo -i yolo_dataset/ -o output.csv

# Конвертировать предсказания (без dataset.yaml) с файлом имён классов
cveta2 convert --from-yolo -i predictions/ -o preds.csv --names-file classes.yaml --image-dir /mnt/data/images
```

Автоматически определяет режим:

- **Режим датасета** — если в директории есть `dataset.yaml`, читает классы и сплиты из него
- **Режим предсказаний** — если `dataset.yaml` нет, обрабатывает `.txt` файлы как предсказания; для имён классов используйте `--names-file`

В режиме предсказаний поддерживается 6-е поле confidence в YOLO `.txt` файлах (формат: `class_id xc yc w h confidence`). Значение записывается в столбец `confidence` выходного CSV.

Изображения автоматически ищутся во всех директориях из `image_cache` конфига. Дополнительные пути можно указать через `--image-dir` (принимает список: `--image-dir d1 d2`).

По умолчанию размеры читаются только у первого изображения — остальные считаются такого же размера. Если изображения в датасете разных размеров, укажите `--read-all-sizes`, чтобы читать размеры каждого файла отдельно.

**CSV → COCO** (`--to-coco`):

```bash
# Конвертировать dataset.csv в COCO detection формат
cveta2 convert --to-coco -d dataset.csv -o coco_dataset/

# Использовать символические ссылки вместо копирования
cveta2 convert --to-coco -d dataset.csv -o coco_dataset/ --link-mode symlink

# Указать дополнительную директорию с изображениями
cveta2 convert --to-coco -d dataset.csv -o coco_dataset/ --image-dir /mnt/data/images
```

Создаёт структуру директорий, совместимую с RF-DETR и другими фреймворками, ожидающими COCO-формат:

```
coco_dataset/
  train/                       -- изображения + аннотации для train
    _annotations.coco.json
    image1.jpg
    image2.jpg
  valid/                       -- изображения + аннотации для val (папка "valid", не "val")
    _annotations.coco.json
  test/                        -- изображения + аннотации для test
    _annotations.coco.json
```

Каждый `_annotations.coco.json` содержит стандартную структуру COCO: `images`, `annotations` (с `bbox` в формате `[x, y, w, h]`), `categories`. Нумерация категорий начинается с 1 (в отличие от YOLO, где с 0). Каждое изображение должно иметь заполненное поле `split` (`train`/`val`/`test`). Сплит `val` сохраняется в папку `valid`.

## Управление проектом

### Управлять метками проекта (`cveta2 labels`)

Просмотр и интерактивное редактирование меток (labels) проекта CVAT. Поддерживает добавление, переименование, изменение цвета и удаление меток. Перед удалением подсчитывает количество аннотаций, использующих метку, и требует явного подтверждения.

```bash
# Показать метки проекта
cveta2 labels -p "Мой проект" --list

# По ID проекта
cveta2 labels -p 123 --list

# Интерактивное редактирование (добавление/переименование/цвет/удаление)
cveta2 labels -p "Мой проект"

# Интерактивный выбор проекта (без -p)
cveta2 labels
```

Операции:

- **Добавление** — создаёт новую метку (цвет назначается CVAT автоматически)
- **Переименование** — безопасно: все аннотации сохраняются (меняется только имя, привязка по ID)
- **Изменение цвета** — безопасно: меняет цвет метки в формате `#rrggbb` (например, `#ff0000`). Не влияет на аннотации
- **Удаление** — **необратимо уничтожает** все аннотации (shapes), использующие метку. Перед удалением команда подсчитывает аннотации по всем задачам проекта и показывает количество. Если аннотации есть — требуется ввести имена меток для подтверждения

### Управлять игнорируемыми задачами (`cveta2 ignore`)

Управление списком игнорируемых задач для проекта. Игнорируемые задачи всегда пропускаются при `fetch` (считаются «в работе»). Задачи можно добавлять/удалять по ID или по имени. Без `--add`/`--remove`/`--list` открывается интерактивное меню.

```bash
# Добавить задачу в ignore-список (по ID)
cveta2 ignore -p "Мой проект" --add 456

# Добавить несколько задач (по ID или имени) с описанием причины
cveta2 ignore -p "Мой проект" --add 456 "Партия 3" -d "Дубликаты"

# Добавить задачу без предупреждения при каждом fetch (--silent)
cveta2 ignore -p "Мой проект" --add 456 --silent

# Удалить задачу из ignore-списка
cveta2 ignore -p "Мой проект" --remove 456

# Показать игнорируемые задачи по всем проектам (не требует подключения к CVAT)
cveta2 ignore --list

# Интерактивный режим (TUI с добавлением/удалением через checkbox)
cveta2 ignore -p "Мой проект"
```

Обычно при `fetch` каждая игнорируемая задача упоминается предупреждением в логе. Флаг `--silent` (в интерактивном режиме — отдельный вопрос) отключает это предупреждение: задача пропускается молча.

Конфигурация хранится в `config.yaml`:

```yaml
ignore:
  my-project:
    - id: 456
      name: "Партия 3"
      description: "Дубликаты"
      silent: true
    - id: 789
      name: "Партия 5"
```

### Операции над задачами (`cveta2 task`)

Групповое пространство команд для точечных операций над одной задачей: `mark-deleted`, `drop-label`, `delete`, `status`. Все подкоманды принимают `--project/-p` (ID или имя; без него — интерактивный выбор) и `--task/-t` (ID или имя задачи). Каждая операция инвалидирует локальный кэш аннотаций задачи.

**Пометить кадры удалёнными** (`task mark-deleted`) — по ID кадра (`--frame`) и/или имени изображения (`--image`); каждый флаг принимает список значений, нужен хотя бы один:

```bash
cveta2 task mark-deleted -p "Мой проект" -t 456 --frame 0 3
cveta2 task mark-deleted -p "Мой проект" -t 456 --image img001.jpg img002.jpg
```

**Удалить аннотации по метке** (`task drop-label`) — **безвозвратно** удаляет на сервере все аннотации задачи с указанной меткой. Показывает количество и требует подтверждения (или флаг `--yes/-y`):

```bash
cveta2 task drop-label -p "Мой проект" -t 456 --label car
cveta2 task drop-label -p "Мой проект" -t 456 --label car --yes
```

**Удалить задачу** (`task delete`) — безвозвратно удаляет задачу целиком, с подтверждением (или `--yes/-y`):

```bash
cveta2 task delete -p "Мой проект" -t 456 --yes
```

**Сменить статус** (`task status`) — устанавливает `--stage` (`annotation`, `validation`, `acceptance`) и/или `--state` (`new`, `in-progress`, `completed`, `rejected`) для **всех jobs** задачи; нужен хотя бы один флаг. Значение `in-progress` соответствует CVAT-состоянию `in progress`:

```bash
cveta2 task status -p "Мой проект" -t 456 --stage acceptance --state completed
cveta2 task status -p "Мой проект" -t 456 --state in-progress
```

## Диагностика

### Проверить конфигурацию (`cveta2 doctor`)

Диагностика конфигурации и окружения. Проверяет:

- Наличие и корректность файла конфигурации (хост, креды)
- Доступность AWS/S3-учётных данных (boto3)
- Права доступа **всех кэшей**: у файлов проверяется групповое чтение и запись, у директорий — групповое чтение, запись и выполнение (чтобы все пользователи группы могли пользоваться кэшем и обновлять его)

Под проверку прав попадают все директории, куда cveta2 пишет кэш: пути из `image_cache`, корни `cache.images_root` / `cache.tasks_root` (включая переопределения по проектам) и собственный каталог `$XDG_CACHE_HOME/cveta2` (`~/.cache/cveta2`) с кэшем аннотаций задач и манифестами незавершённых загрузок. Вложенные друг в друга корни обходятся один раз; файлы конфигурации не проверяются — они у каждого пользователя свои.

```bash
cveta2 doctor
```

#### Починить права (`cveta2 doctor --cache`)

Флаг `--cache` не только проверяет, но и **чинит** права: каждому найденному пути добавляются недостающие групповые биты (`rwx` для директорий, `rw` для файлов). Остальные биты не трогаются — команда только расширяет доступ группе, ничего не открывая «всем».

`chmod` разрешён только владельцу файла, поэтому починить чужие пути невозможно. Такие пути команда перечисляет и в конце выводит **список пользователей, которым нужно самим выполнить `cveta2 doctor --cache`**:

```
cache.images_root (/mnt/data/images): 2 dir(s) and 5 file(s) not group-accessible. Owners: alice (4 items), bob (3 items)
  drwx------  alice  /mnt/data/images/projA/2026-01
  …
These users own the remaining paths and must run 'cveta2 doctor --cache' themselves: alice (4 items), bob (3 items)
```

```bash
cveta2 doctor --cache
```

Если каталог невозможно прочитать (нет группового `x`, а владелец — другой пользователь), он попадает в отчёт, но его содержимое не проверяется: после того как владелец выполнит `doctor --cache`, запустите команду ещё раз, чтобы обойти открывшееся поддерево.

> **Групповые биты — только половина дела.** Права дают доступ той группе, которой файл принадлежит, а новый файл получает **основную группу своего создателя**. Чтобы кэш действительно был общим, у всех пользователей должна быть общая группа, и корень кэша обычно помечают `setgid`, чтобы она наследовалась на всё содержимое:
>
> ```bash
> sudo chgrp -R cvat-users /mnt/data/images   # общая группа
> sudo chmod g+s /mnt/data/images             # setgid: наследование группы
> ```
>
> Сам cveta2 группу и `setgid` не меняет — это решение администратора машины.

